<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#000000"/> <!-- Adjust the opacity here (40 is the opacity) -->
    <corners android:topLeftRadius="@dimen/_10ssp"
        android:bottomLeftRadius="@dimen/_10ssp"
        android:topRightRadius="@dimen/_10ssp"
        android:bottomRightRadius="@dimen/_10ssp"/> <!-- Set the border radius (adjust the value as needed) -->
</shape>
