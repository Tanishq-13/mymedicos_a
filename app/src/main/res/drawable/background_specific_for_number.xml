<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Add solid fill with white color -->
            <stroke android:color="@color/unselected" android:width="1dp" /> <!-- Add stroke with color and width -->
            <corners android:radius="@dimen/_10ssp" />
        </shape>
    </item>
</selector>
